(function() {
    'use strict';

    $( document ).ready(function() {


        // add fluid class to widen the page
        jQuery('div[role=main]').removeClass('container');
        jQuery('div[role=main]').addClass('container-fluid');
        
        var main = jQuery('div[role=main]')

    });

})();
